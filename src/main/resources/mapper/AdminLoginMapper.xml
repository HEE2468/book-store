<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
        "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sww.bank_book.admin.dao.AdminLoginDao">

    <select id="adminLogin" resultType="com.sww.bank_book.admin.entity.Admin">
        SELECT
        id AS adminId,
        admin_name AS adminName,
        admin_password AS adminPassword
        FROM admin
        WHERE admin_name = #{adminName} and admin_password = #{adminPassword}
    </select>

    <select id="findBookCategoryOfNumber" resultType="com.sww.bank_book.admin.entity.CategoryNameList">
        SELECT
        bookCategory.category AS name,
        COUNT(bookCategory.category) AS value
        FROM book_category AS bookCategory
        LEFT JOIN book_Details AS bookDetails ON bookDetails.book_category_id = bookCategory.book_category_id
        GROUP BY bookCategory.category
    </select>

</mapper>